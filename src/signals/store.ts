// Placeholder for durable signal persistence. The first implementation keeps live
// state in memory and recovers yielded runs through the existing reliability store.
export type PiboSignalStore = never;
