# Pibo Wrapper V1

This run is started through the pibo wrapper, not the raw Pi CLI.

V1 keeps the wrapper small:
- load the selected skill profile
- attach explicit context files
- leave project-owned custom tools empty for now
- use Pi as the inner agent runtime
